Please build LandingPage and SEAD-NDS-Repository(https://github.com/Data-to-Insight-Center/SEAD-NDS-Repository) first. 


Step 1:

Set proper values for properties in sead2/sda-agent/config/config.properties file

Step 2:

Run standalone build script

./build-standalone.sh.

This will create a shell script bin/iusc-azure-agent.sh which can be used to start the agent.


Step 3:

Run bin/iusc-azure-agent.sh to start IU SEAD Cloud Azure agent. You have to provide the 'config.properties' file path as a parameter.

nohup ./bin/iusc-azure-agent.sh config/config.properties > log.txt &

